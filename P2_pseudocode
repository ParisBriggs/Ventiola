// Pseudocode for Ventiola Prototype II 

Include libraries 
Define variables 

Setup() {
  // setup lilypad outputs: speaker 
  // setup lilypad inputs: soft pots, pressure sensor 
}

Loop() {
  Setup(); 
  readSensor(); 
  
  if (sensorVal) { // only play note if pressure applied to sensor 
    readSoftPots(); // soft pots determine the musical note 
    pitchBendNote(sensorVal); // pressure sensor determines the pitch bend of the note (octave shifting) 
  }
  else { 
    return;
  } 


}
