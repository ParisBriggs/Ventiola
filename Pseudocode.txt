// Pseudocode for Ventiola Prototype II 

// Very rudimentary pseudocode!! 

Include libraries 
Define variables 

Setup() {
  // setup lilypad outputs: speaker 
  // setup lilypad inputs: soft pots, pressure sensor 
}

Loop() {
  Setup(); 

  uint16 sensorVal = readSensor(); // read in pressure sensor value 
  uint16 note; 
  
  if (sensorVal) { // only play note if pressure applied to sensor 
    note = readSoftPots(); // soft pots determine the musical note 
    pitchBendNote(sensorVal, note); // pressure sensor determines the pitch bend of the note (octave shifting) 
  }
  else { 
    return;
  } 
}

readSensor() {}
readSoftPots() {}
pitchBendNote(sensorVal, note) {} 



